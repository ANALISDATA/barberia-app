# Copia este archivo a secrets.toml (en esta misma carpeta .streamlit) y completa
# los valores desde Supabase → Project Settings → API.
#
# secrets.toml NUNCA se sube a GitHub (ya está en .gitignore).

supabase_url = "https://xxxxxxxx.supabase.co"
supabase_key = "PEGAR-AQUI-LA-CLAVE-service_role-NO-LA-anon"

# Contraseña para entrar al panel de administrador (la que tú quieras, la inventas).
admin_password = "cambia-esta-clave"
